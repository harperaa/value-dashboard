"""value-dashboard — hermes plugin entry point (dashboard page only)."""
from __future__ import annotations


def register(ctx) -> None:  # no chat tools yet — the page is the product
    return None
